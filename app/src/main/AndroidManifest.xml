<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mplayer">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/mplayer_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".structure.body.management.activities.SelectSetupActivity"></activity>
        <activity android:name=".structure.body.management.activities.NewSetupActivity" />
        <activity android:name=".structure.body.SelectActivity" />
        <activity android:name=".structure.body.management.activities.SettingsActivity" />
        <activity android:name=".structure.body.management.activities.ManagePlaylistActivity" />
        <activity android:name=".structure.body.management.activities.ManageSetupActivity" />
        <activity android:name=".structure.body.management.activities.ManageDeviceActivity" />
        <activity android:name=".structure.body.BaseActivity" />
        <activity android:name=".structure.login.SignUpActivity" />
        <activity android:name=".structure.player.PlayerActivity" />
        <activity android:name=".structure.login.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>